/** Fallback-Höhe in CSS, bis die Nav per ResizeObserver gemessen wird. */
export const PWA_BOTTOM_NAV_HEIGHT = "5.5rem";

/** Zusätzlicher Abstand, damit letzte Inhalte nicht unter der Nav liegen. */
export const PWA_BOTTOM_NAV_INSET_BUFFER_PX = 12;

export const PWA_MOBILE_NAV_MQ = "(max-width: 1023px)";
